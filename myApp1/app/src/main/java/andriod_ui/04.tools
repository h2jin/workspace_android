

app 폴더 밑에 src 폴더

androidTest -> 테스트코드 사용시 쓰는 폴더

main -> 우리가 작성하는 자바코드(코틀린)을 작성할 때 사용한다.

res-> resource의 약자 (이미지, 필요한 여러 utils 파일 등을 넣어두는 곳)

drawable -> 이미지를 넣어두는 폴더이다.
layout -> xml을 이용해서 화면을 그릴때 사용하는 파일을 넣어둔다.
mipmap -> 앱에 아이콘 이미지를 넣어두면 된다.
values -> 공통적으로 사용되는 값들을 넣어두는 곳. (디파인)